local toolchain_config = jst.env('TOOLCHAIN_CONFIG', default={});

{
  defaults: {
    type: @'//CC/pkgconfig:defaults',
    arguments_config: ['TOOLCHAIN_CONFIG'],
    'pkg-config': ['pkg-config'],
    PKG_CONFIG_PATH: jst.get('PKG_CONFIG_PATH', toolchain_config, default=[]),
  },
}
